# Proyecto de Aula - Business Analytics (Streamlit)
Actividad final: Disenando el Futuro con Analisis Prescriptivo

Ejecutar:
  python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
  pip install -r requirements.txt
  streamlit run streamlit_app.py

La base esta embebida en base.xlsx.